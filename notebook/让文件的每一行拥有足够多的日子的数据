比如第一行有第一天到第9天的数据(另外还要额外的5行数据,作为 output)

    最终文件的每一行要有一个对象，它负责搞出这一行最终的文本，

    在遍历原文件的每一行的时候，构造一个新的这个对象，把这行文本作为构造函数的参数，另外一个参数：这个对象需要搞出的是·几天·的文本

    同时，之前的所有这种对象放在list中，把新这行的文本内容feed给list中的所有的对象，那些对象自己决定要不要接受，它使这样决定的：自己已经记录了多少天的文本了，如果不够，就接受，够，就丢弃。

    然后把新的对象也放到list中去。

    直到原文件的最后一行也这样处理完了.

    然后遍历 list 中的所有的对象,如果对象记录的数据是完整的(天数是否足够),就把它的文本输出到新文件中去.



